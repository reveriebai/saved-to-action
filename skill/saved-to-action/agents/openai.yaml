interface:
  display_name: "Saved to Action"
  short_description: "把 Markdown 笔记提炼成可执行的本地行动看板"
  default_prompt: "Use $saved-to-action to inspect my Markdown folder and set up a private local action board after I confirm the paths."
policy:
  allow_implicit_invocation: false
